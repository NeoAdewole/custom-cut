<?php

function customcut_register_assets()
{
  // use for scripts and styles scoped to just the block editor scoped 
  /* $editorAssets = include(CUSTOMCUT__BLOCK_DIR . 'build/block-editor/index.asset.php');

  wp_register_script(
    'customcut-editor',
    CUSTOMCUT__BLOCK_DIR . '/build/index.js'
  );

  wp_register_style(
    'customcut-editor',
    CUSTOMCUT__BLOCK_DIR . '/build/editor.css'
  ); */
}
